# aioamazondevices requires Python >= 3.12. Use Raspberry Pi OS Trixie
# (ships Python 3.13). Pin a known-good version for appliance stability;
# bump deliberately and re-verify the passkey setup flow when you do.
aioamazondevices==14.1.3
aiohttp
fastapi
uvicorn[standard]
jinja2
python-multipart
python-dotenv
pydantic
mfrc522
RPi.GPIO
evdev
